{
  "name": "local-coder",
  "version": "1.0.0",
  "description": "Configure OpenCode to use capable local models through Ollama",
  "type": "module",
  "bin": { "local-coder": "bin/local-coder", "setup-ai": "bin/local-coder" },
  "files": ["bin", "dist", "catalog"],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "prepare": "npm run build"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "@clack/prompts": "^0.11.0", "jsonc-parser": "^3.3.1" },
  "devDependencies": { "@types/node": "^22.15.0", "typescript": "^5.8.3" },
  "license": "MIT"
}
