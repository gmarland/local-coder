{
  "name": "localstack",
  "version": "1.0.0",
  "description": "LocalStack configures OpenCode to use capable local models through Ollama",
  "type": "module",
  "bin": { "localstack": "bin/localstack", "setup-ai": "bin/localstack" },
  "files": ["bin", "dist", "catalog"],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "prepare": "npm run build"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "@clack/prompts": "^0.11.0", "jsonc-parser": "^3.3.1" },
  "devDependencies": { "@types/node": "^22.15.0", "typescript": "^5.8.3" },
  "license": "MIT"
}
